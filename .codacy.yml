---
engines:
  pylint:
    exclude_paths:
      - "**/migrations/**"
  pylintpython3:
    exclude_paths:
      - "**/migrations/**"
  ruff:
    exclude_paths:
      - "uv.lock"
      - "**/requirements*.txt"
      - "core_layer/requirements.txt"
      - "gps_layer/requirements.txt"
      - "sensor_layer/requirements.txt"

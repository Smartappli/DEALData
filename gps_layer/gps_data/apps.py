"""Application configuration for the gps_data app."""

from django.apps import AppConfig


class GpsConfig(AppConfig):
    """Configuration for the gps_data application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "gps_data"

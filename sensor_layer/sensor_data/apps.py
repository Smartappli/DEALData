"""Application configuration for the sensor data app."""

from django.apps import AppConfig


class SensorDataConfig(AppConfig):
    """Configure the sensor data Django application."""

    name = "sensor_data"
